{
  "name": "auracloud-monorepo",
  "private": true,
  "engines": {
    "node": ">=22.12"
  },
  "workspaces": [
    "utils",
    "logic",
    "crawlers",
    "api-server",
    "mcp-server",
    "frontend"
  ],
  "scripts": {
    "test": "vitest run",
    "start": "sh -c 'if [ -z \"$1\" ]; then echo \"Usage: npm start <workspace>\"; exit 1; fi; npm run dev -w \"$1\"' --",
    "build": "sh -c 'if [ -z \"$1\" ]; then echo \"Usage: npm run build <workspace>\"; exit 1; fi; npm run build -w \"$1\"' --",
    "build:all": "npm run build -w utils && npm run build --workspaces --if-present",
    "clean": "rm -rf */dist */tsconfig.tsbuildinfo",
    "start:utils": "npm run dev -w utils",
    "start:logic": "npm run dev -w logic",
    "start:crawlers": "npm run dev -w crawlers",
    "start:api-server": "npm run dev -w api-server",
    "start:mcp-server": "npm run dev -w mcp-server",
    "start:mcp-http": "npm run dev:http -w mcp-server",
    "start:all": "npm run redis:up && npx concurrently -n crawlers,logic,api,mcp,frontend -c blue,magenta,green,yellow,cyan \"npm run dev -w crawlers\" \"npm run dev -w logic\" \"npm run dev -w api-server\" \"npm run dev:http -w mcp-server\" \"npm run dev -w frontend\"",
    "start:no-crawlers": "npm run redis:up && npx concurrently -n logic,api,mcp,frontend -c magenta,green,yellow,cyan \"npm run dev -w logic\" \"npm run dev -w api-server\" \"npm run dev:http -w mcp-server\" \"npm run dev -w frontend\"",
    "redis:up": "npm run redis:up -w utils"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.1106.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "vitest": "^4.1.10"
  }
}
